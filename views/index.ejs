<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <% for (var i = 0; i < items.length; i++) { %>
      <div class="meshi-box">
        名称: <a href="<%= items[i].url %>"><%= items[i].name %></a>
        営業時間：<%= items[i].opentime %>
      </div>
    <% } %>

  </body>
  <script type="text/javascript">
    // TODO: 位置情報を取得する
    // TODO: 位置情報を基に、Ajaxで店舗一覧をとってくる

  </script>
</html>
